#include <stdio.h>
/**
 * main - num arg
 * @argc: string
 * @argv: whatever
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int i = 0;
	(void) argv;

	for (i = 0; i < argc - 1; i++)
	{

	}

	printf("%d\n", i);
	return (0);
}
