#include "main.h"
/**
 * _strlen - length of string
 * @s: length of a string
 * Return: length of string
 */
int _strlen(char *s)
{
	int i;

	s[1000];
	for (i = 0; s[i] != '\0'; i++)

	return (i + '\0');
}

